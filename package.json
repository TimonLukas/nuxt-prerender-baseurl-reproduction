{
  "private": true,
  "scripts": {
    "generate-good": "nuxt generate",
    "generate-bad": "NUXT_APP_BASE_URL=/foobar/ nuxt generate",
    "generate-workaround": "NUXT_APP_BASE_URL=/foobar/ WORKAROUND=true nuxt generate"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.1"
  }
}
